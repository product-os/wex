{
  "experiments": [
    {
      "it": "Detects when PR is merged",
      "pull_request": {
        "event": "webhook.json",
        "test": {
          "includes": ["PR was merged"]
        }
      }
    }
  ]
}
